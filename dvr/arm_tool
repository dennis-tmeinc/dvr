
# cross compile toolchain
ifeq ($(EAGLE),eagle34)
#CROSS_COMPILE_PREFIX=$(HOME)/proj/eagle34/hiktoolchain/bin/arm_v5t_le-
CROSS_COMPILE_PREFIX=$(HOME)/proj/eagle34/eagle34SDK/v5t_le-mv401_uc/bin/arm_v5t_le-
# this is rebuilt toolchain
# CROSS_COMPILE_PREFIX=/home/dennis/toolchain/bin/arm-linux-
else ifeq ($(EAGLE),eagle32)
CROSS_COMPILE_PREFIX=$(HOME)/proj/eagle32/hik_toolchain/bin/arm-linux-
else ifeq ($(EAGLE),mv)
CROSS_COMPILE_PREFIX=$(HOME)/bin/cross-arm-mv5sft/bin/arm-mv5sft-linux-gnueabi-
#CROSS_COMPILE_PREFIX=$(HOME)/proj/eagle34/eagle34SDK/v5t_le-mv401_uc/bin/arm_v5t_le-
else ifeq ($(EAGLE),zeus3)
CROSS_COMPILE_PREFIX=$(HOME)/proj/zeus3/v5t_le/bin/arm_v5t_le-
else
CROSS_COMPILE_PREFIX=	
endif

export CROSS_COMPILE=$(CROSS_COMPILE_PREFIX)
export CC=$(CROSS_COMPILE_PREFIX)gcc
export GPP=$(CROSS_COMPILE_PREFIX)g++
export STRIP=$(CROSS_COMPILE_PREFIX)strip

# debugging folder
debug_folder=/home/dennis/nfsroot/eagletest
